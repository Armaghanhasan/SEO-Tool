// Centralized configuration for the application

/**
 * The email address of the default administrator account.
 * This account will have access to all tools and the Admin Panel by default.
 */
export const ADMIN_EMAIL = 'armaghan.hhasan@gmail.com';

/**
 * The password for the default administrator account.
 * IMPORTANT: This is stored in plain text in the code and is NOT secure.
 * For a production application, authentication should be handled by a secure backend.
 */
export const ADMIN_PASSWORD = 'Myconputer@smar8';
